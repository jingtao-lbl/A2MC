[pytest]
# Keep pytest's tmp_path dirs under the repo (gitignored tmp/) — never system /tmp
# (NERSC no-writes-outside-home rule). Run: ~/a2mc_env/bin/python -m pytest
addopts = --basetemp=tmp/pytest -q
testpaths = tests
